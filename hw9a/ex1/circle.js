const getCircumference = radius => { return 2 * Math.PI * radius; };
const getArea = radius => { return Math.PI * radius * radius; };

module.exports.getCircumference = getCircumference;
module.exports.getArea = getArea;
