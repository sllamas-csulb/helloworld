const circle = require("./circle.js");

let radius = 3;

console.log( "The circumference of a circle of radius " + 3 + " is " + circle.getCircumference( radius ) );
console.log( "Its area is " +  + circle.getArea( radius ) ); 
